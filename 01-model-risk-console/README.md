# Moved

This folder was an earlier draft of Project 01. The current Project 01 lives at:

[`../01-model-drift-sentinel/`](../01-model-drift-sentinel/)

The reframe: the original "Model Risk Governance Console" was a workflow play. The current version, Production Model DriftSentinel, focuses on the bleeding problem first — silent decay of deployed models — and lets the governance plug into the existing MRM workbench rather than competing with it. Same author, sharper wedge.
